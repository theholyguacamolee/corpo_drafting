# GEMINI_API_KEY: Required for all AI features across the suite
# (SPA/Secretary's Certificate refine + clause extraction, and
# Articles of Amendment baseline extraction from uploaded PDFs).
GEMINI_API_KEY=

# Port for the local dev server (defaults to 3000 if unset)
PORT=3000
